# Product Infrastructure

This folder holds deployment and operations references for the product runtime.

The product root `../docker-compose.yml` is the canonical local runtime
entrypoint for LearnLab Bridge main core.

It also holds the product-local JupyterHub/JupyterLab runtime configuration,
starter content, and local runtime data directories used by Docker Compose.
